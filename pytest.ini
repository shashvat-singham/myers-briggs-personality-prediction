[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    models: requires the real joblib artifacts (scikit-learn 0.23 / Python 3.8)
filterwarnings =
    ignore::DeprecationWarning
